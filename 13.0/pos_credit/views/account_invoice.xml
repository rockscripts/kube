<odoo>
  <data>
    <record id="account_invoice_form_for_credits" model="ir.ui.view">
        <field name="name"></field>
        <field name="model">account.move</field>
        <field name="type">form</field>       
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="priority" eval="8"/>
        <field name="arch" type="xml">
            <field name="ref" position="before">
                <field name="business_transaction_type" readonly="1"/>
            </field>
            <xpath expr="//page[last()]" position="after">
                <page string="Fiados / Crédito" name="credit_payment_terms">      
                    <group>
                        <!--
                        <field name="fixed_period" readonly="1"/>                        
                        <field name="payment_terms_cuotes" attrs="{'invisible': [('fixed_period','=',False)]}"/>
                        <field name="payment_terms_period" attrs="{'invisible': [('fixed_period','=',False)]}"/>
                        <field name="payment_id"/>
                        <field name="payment_terms"/>
                        <field name="credit_amount_fixed" widget='monetary'/>
                        -->
                    </group>
                </page>
            </xpath>
        </field>
    </record>
  </data>
</odoo>
